
default:
  lc0_version: "0.24.1"
  tag_suffix: ""
  imagename: simske/lc0
  stockfish: False
  build:
    image: nvidia/cuda:10.0-cudnn7-devel
    apt_prerequisites: wget ninja-build python3 python3-pip
    python3_link: True

  runtime:
    image: nvidia/cuda:10.0-cudnn7-runtime
    apt_prerequisites: wget
    shared_libraries:
     - /root/lc0/build/release/./subprojects/zlib-1.2.11/libz.so
    cmd: True

default_stockfish:
  base: default
  stockfish: True
  tag_suffix: -stockfish

vastai:
  base: default
  tag_suffix: -vastai

vastai_stockfish:
  base: vastai
  tag_suffix: -stockfish-vastai
  stockfish: True
